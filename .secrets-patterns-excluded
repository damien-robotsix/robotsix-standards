# Patterns for secret scanners (detect-secrets, TruffleHog) to skip.
# One gitignore-style glob per line. See docs/secret-files-are-never-tracked.md.
# This is a docs-only repo — add patterns here when a tracked file triggers
# a false positive, rather than adding it to .gitignore.
