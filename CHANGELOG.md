## 0.0.0 (unreleased)

- Document the SBOM artifact (CycloneDX), TruffleHog secret scanning, and
  pip-audit CVE audit in the security gate set — reconciling the standards
  wording with what the shared `python-security.yml` workflow actually runs.
