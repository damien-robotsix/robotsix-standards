Removed `project_id` from the canonical Langfuse block in component-standard.md. Clarified that `project_id` is NOT part of the component's config model — it is added by the deployment engine through Langfuse API enrichment. Updated "Failure prevented" prose accordingly. Resolves internal inconsistency with config-standard.md §7, whose `LangfuseProjectConfig` model has no `project_id`.
