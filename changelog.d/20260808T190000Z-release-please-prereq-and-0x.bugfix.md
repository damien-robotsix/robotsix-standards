Documented the "Allow GitHub Actions to create and approve pull requests" repository setting as a prerequisite for release-please — without it every run fails at PR creation, which is why release-please had produced nothing across the fleet. Also added the missing `bump-minor-pre-major` flag to the config example: rule 4 requires staying in the 0.x line, but the printed config omitted the only setting that enforces it, so following the page exactly produced the `1.0.0` bump the page forbids.
