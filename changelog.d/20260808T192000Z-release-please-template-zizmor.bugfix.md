Aligned the release-please workflow template with the fleet's own `lint-workflows` audit. As printed it failed zizmor on four counts — workflow-level write permissions (`excessive-permissions`), undocumented permissions, an unnamed job (`anonymous-definition`), and a missing `concurrency` block — so a repo following the page exactly could not pass CI. Also added `workflow_dispatch`, and a note that migrating repos must bump their `baseline-check.yml` pin to `a6378ac` or later.
